# Additional requirements for schema matcher automation
# Install with: pip install -r requirements_automation.txt

# Core dependencies (add to main requirements.txt if not already there)
pandas>=1.5.0
psycopg2-binary>=2.9.0
pymongo>=4.0.0
python-dotenv>=0.19.0

# Automation-specific dependencies
PyYAML>=6.0
croniter>=1.3.0

# Optional: Enhanced logging and monitoring
colorlog>=6.7.0

# Optional: Email notifications
# smtplib is built-in, no installation needed

# Optional: System monitoring
psutil>=5.9.0